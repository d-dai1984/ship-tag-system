# 项目 Hand-off

> 更新时间：2026-09-15
> 当前主线：产品结构已按外部 V0.4 来源重新梳理，范围从"资产标签台账"扩张为"点检闭环 + 三层角色看板 + 环境监测 + 移动端"，并规划二期设备调试管理。
> 当前状态：步骤 0～⑤ 全部完成。三个 HTML（管理后台原型 / 产品结构说明 / 点检员移动端）均对齐 v0.4 并互链。

## 原型改造进度（按 2026-09-15 商定的顺序）

| 步骤 | 内容 | 状态 |
|---|---|---|
| 0 | 四级目录 | ✅ 已完成并验证 |
| ① | 点检 + 环境数据模型、三层视角角色 | ✅ 已完成并验证 |
| ② | 看板中心：三层角色 × 三级钻取 | ✅ 已完成并验证 |
| ③ | 嵌入文档切 v0.4 + `build.py` 与需求映射重建 | ✅ 已完成并验证 |
| ④ | 点检引擎页 / 系统监控看板 / 环境监测页 + 模块重排 + 结构说明页 | ✅ 已完成并验证 |
| ⑤ | 移动端 · 独立文件 `mobile.html` | ✅ 已完成并验证 |

## 本轮结论（2026-09-15）

一轮做完了整个 v0.4 迁移。下面是**接手者只读这一段也够用**的结论；逐步骤的实施细节在后面各章。

### 1. 产品基线换了

从「资产标签台账」扩张为「**点检闭环**」。v0.1 明确把点检、移动端列为后续扩展，v0.4 把它们拉回一期核心，并新增三层角色看板、环境监测，二期规划设备调试管理。

这不是修订，是范围扩张。v0.1 保留为历史版本。

### 2. 三个交付物，一套来源

| 文件 | 内容 |
|---|---|
| `prototype.html` | 管理后台：6 个模块、17 个视图 |
| `mobile.html` | 点检员移动端：3 个标签页 |
| `product-structure.html` | 结构说明页：9 个模块、114 条需求，可打印 |

三者的需求原文都由 `prototype/docparse.py` 从同一份 v0.4 文档生成，**同源同 id**，顶部三方互链。分享时三个文件必须放同一文件夹。

### 3. 顺手根治了一处结构性脆弱

旧的需求映射用**零起始索引**（`'1-3'`），文档表格增删一行就整体错位——而且是**静默错位**：构建照样成功，只是每条需求都指错功能。

改成 **id 键**（模块 slug / 功能名）+ 构建期校验。实测两种改动：

| 改动 | 结果 |
|---|---|
| 改功能名 | 构建**失败**，报出失配 id，退出码 1 |
| 表格中间插一行 | 构建**通过**，映射不受影响 |

从此只有改功能名才会失配，而且会当场炸出来。

### 4. 几条设计原则（已写入项目记忆第 3.0 节）

- **派生值不落库**：点检状态由「保养周期 + 上次点检时间」相对演示基准时间推算；告警同理，只有「处理状态」落库。避免状态字段与来源数据不一致。
- **演示基准时间**固定为 `2026-09-15 10:00:00`，原型在任何日期打开都是同一套状态，不随真实时间漂移。
- **三层角色用切换器而非三个页面**：一键切换才能演示「同一份数据、三个视角」。
- **功能权限角色与看板视角是两个维度**，可自由组合。
- **一次点检必须拆成三段**：手机本地已保存 / 边缘服务已接收 / 标签已刷新。前一段完成不代表后一段完成。
- **跨舱室的系统（压载、消防）不靠目录表达**，用标签分组表达。目录管主归属，分组管跨目录批量操作。

### 5. 原型把三条风险变成了可点击的演示

之前这些只是文档里的句子，现在当场能看见：

| 风险 | 在哪看得见 |
|---|---|
| 标签在线 ≠ 屏幕已更新 | 移动端完成页的三段闭环。标签离线时第 3 段停在「待刷新」，不谎报成功 |
| 周期一改，正常设备立刻变逾期 | 点检引擎页改保养周期 30→10 天，状态当场从「正常」翻成「逾期」，标签转为待更新 |
| 被监测环境超出监测终端工作范围 | 环境监测页主机舱 72% RH、双层底 85% RH，均已超出候选标签自身 45–70% RH 的工作范围 |

### 6. 没有粉饰的地方

- 移动端 NFC 是**手动选标签模拟**，三处明写不构成 NFC 可用的证据；需求页标「依赖未落实」。
- 需求面板有「原型未实现」灰标，v0.4 写了但原型没做到的会明确标出来，不假装覆盖。
- 114 条需求中管理后台覆盖 77 条，移动端 7 条里 5 条可演示、2 条依赖未落实。未覆盖的主要是二期调试管理。

### 7. 剩下的不是原型问题

原型能做的已经做完。下面几条**没有业务方答案就无法继续**，再迭代原型也不会更接近真相：

1. **标签 NFC 是否落实** —— 全局单点依赖：移动端防假检、免密唤起、二期上电确认全压在这一条上。规格书里它只是「可选」。
2. **现场环境与危险分区** —— 标签无 IP 等级、工作范围 0–40°C / 45–70% RH，与船厂工况和自身的环境监测场景都冲突。
3. **统一负载模型下的实测续航** —— 5 年是理论值，v0.4 新增的 LED 闪烁与动态倒计时重刷全未计入，当前不能对外承诺。
4. **二期电流传感器 / PLC 接入是否立项** —— 二期不是软件扩展，运行时长与上电检测标签做不到。不落实就只能降级为「NFC 打卡审计」。
5. **产品是否正式改名**、目录是否需要「总段」层、编码规则是否与船厂现行一致。

完整清单见 `PROJECT_MEMORY.md` 第 9 章。

---

## 本轮（2026-09-15）：产品结构重梳理 v0.4

来源：用户提供的 DeepSeek 分享对话，原文已存档 [`docs/参考-DeepSeek输出-产品结构V0.4-2026-09-15.md`](docs/参考-DeepSeek输出-产品结构V0.4-2026-09-15.md)。该来源是外部 AI 生成的讨论稿，不是业务方批准的规格。

产出：[`船舶资产智能点检标签管理系统-产品结构与功能范围-v0.4.md`](船舶资产智能点检标签管理系统-产品结构与功能范围-v0.4.md)

结构变化（详见该文第 9 章）：

- 新增一级模块：点检引擎、环境监测、移动端；二期新增设备调试管理。
- 全船看板 → 看板中心，重构为"三层角色 × 三级钻取"，并新增第二页系统监控看板。
- 标签状态从一级模块降级，拆入系统监控看板与标签台账详情页。
- 仓储管理 → 舱室/部位管理，层级口径绑定造船分段编码体系。
- 产品名称在来源中改为"船舶资产智能点检标签管理系统"，**待业务方确认**，确认前不批量改名。

未做（需要下一轮决策后再动）：

- `prototype.html` 及 `prototype/` 源码仍是 v0.1 的 5 模块 13 视图；除目录层级外未按 v0.4 改造（点检引擎、三层角色看板、系统监控看板、环境监测、移动端均未实现）。
- 移动端未做的：真实 NFC 唤起、真实拍照与图片上传、后台定时补传、工单状态机、MES/EAM 对接。变屏变灯是界面模拟，不连接现场设备。
- `PROJECT_MEMORY.md` 中硬件结论未升级：v0.4 是讨论稿，第 10 章的 ❌/⚠️ 项在取得书面依据前维持原判。

## ⑤ 点检员移动端（2026-09-15，已完成并验证）

按先前商定，**没有塞进桌面原型**，而是独立文件 `mobile.html`（源码 `mobile/`，构建 `prototype/build_mobile.py`）。理由：手机形态、NFC 唤起、离线补传跟桌面后台是两种交互模型，混在一个文件里两边都做不好。

### 两个独立缓存域

| 存储键 | 内容 |
|---|---|
| `ship-tag-prototype-v1` | 业务数据，与管理后台原型**共用**（同源部署时是同一份） |
| `ship-tag-mobile-v1` | 手机离线队列与网络状态，**单独存** |

分开不是图省事：v0.4 文档里就写明「离线移动端与离线网关是两个互不相关的缓存域，必须分别设计记录 ID、版本、幂等键与冲突规则」。手机队列属于手机侧，本来就不该混进业务数据。

管理后台没跑过时，移动端用内置 fallback 示例（字段与 v4 结构一致），并在首页明示「未读到管理后台数据」。

### 页面

三个标签页：**今日点检** / **离线队列** / **需求对照**。

- 首页：碰一碰入口、逾期/临期/待补传三个计数、待办设备列表，以及一张「防假检机制成不成立」的证据链卡片。
- 表单：**按设备类型加载检查项**（泵 / 发电机 / 雷达 / 配电柜 / 锚机 / 滤器 / 通用共 7 套），任一项标异常即强制填描述 + 拍照，可勾选转报修工单。
- 完成页：墨水屏与 LED 的现场反馈模拟 + **三段闭环状态**。
- 队列：在线/离线切换，恢复网络自动补传，每条记录显示上报与刷屏状态。

### 三段闭环是这一版的重点

完成页把一次点检拆成三段分别显示，**前一段完成不代表后一段完成**：

1. 手机本地已保存 —— 断网也不会丢
2. 边缘服务已接收 —— 离线时停在这里等补传
3. 标签已刷新 / 已点灯 —— 标签离线时即使已上报也到不了这一步

实测三种结果：离线提交 → 只亮第 1 段；恢复网络补传但标签离线（滑油泵 / l3）→ 亮 1、2 段，第 3 段停在「待刷新」；在线且标签在线（燃油输送泵 A / l1）→ 三段全亮，绿灯 3 秒，墨水屏红条消失。

这把「标签在线 ≠ 屏幕已更新」从文档里的一句话，变成了当场能看见的三个状态。

### 对 NFC 的处理

**没有假装 NFC 可用。** 碰一碰在原型里是手动选标签，并在三处明写这一点：侧栏警示块、模拟碰一碰弹层顶部、需求对照页的「依赖未落实」标签。

需求对照页 7 条需求按实现状态标注：NFC 碰一碰唤起、防假检机制 = **依赖未落实**（界面能走通，真机能否成立取决于标签 NFC）；其余 5 条 = 原型可演示。`build_mobile.py` 里有构建期校验，文档新增移动端需求却没标状态会直接构建失败。

### 验证

| 项 | 结果 |
|---|---|
| 三个标签页 + 弹层 + 表单 | 390px 无横向溢出，无控制台错误 |
| 防呆校验 | 异常项缺描述、缺拍照各自被拦住 |
| 离线 → 恢复网络 | 自动补传 1 条，点检流水 +1、lastCheck 更新、标签 version +1 |
| 标签离线场景 | 第 3 段正确停在「待刷新」，未谎报成功 |
| 标签在线场景 | 三段全亮，published 跟上 version |
| 三页互链 | 管理后台 ↔ 结构说明 ↔ 移动端，六个方向都通 |

## ④ 三个新页面 + 模块重排 + 结构说明页（2026-09-15，已完成并验证）

### 导航结构（数据结构版本 `3` → `4`）

```
看板中心      tabs: 业务看板 / 系统监控看板     ← 系统监控看板为新增
舱室 / 部位管理                                ← 由「仓储管理」改名
标签管理      tabs: 标签台账 / 标签状态 / 标签分组 / 字段与数据 / 模板设计 / 更新任务 / 标签策略
点检引擎      tabs: 周期与预警规则 / 点检流水    ← 全新模块
环境监测                                       ← 全新模块
系统管理      tabs: 网关管理 / 账号权限 / 基础设置 / 操作日志
```

**「标签状态」从一级模块降为标签管理下的标签页**。v0.4 的模块清单里没有这个模块，它被拆进系统监控看板与标签台账详情。功能一条没删，只是归位——否则它会和新增的系统监控看板明显重叠。

### 新增能力

| 页面 | 内容 |
|---|---|
| 看板中心 · 系统监控看板 | 标签运行监控（在线率/离线/低电/弱信号/刷新失败）、网关运行监控（连接数、上下行、缓存积压）、系统健康度、**统一告警中心** |
| 点检引擎 · 周期与预警规则 | 分级预警规则表（v0.4 三档口径）、按截止时间排序的保养周期表，可直接改周期 |
| 点检引擎 · 点检流水 | 可搜索、可导出 CSV 的审计流水 |
| 环境监测 | 采集点实时值（可改实测值模拟超限）、超限告警、**高湿联动**（预览逐台影响后再应用） |

**告警中心是统一入口**，同时承载业务告警（点检逾期、未首检、故障报修、环境超限）与系统告警（标签离线/低电/弱信号、网关离线、刷新失败），共 4 个级别、3 种处理状态。告警本身由数据推算，只有「处理状态」落库——和点检状态同一个原则。

### 两条改动会连锁的链路（实测）

改保养周期 30→10 天：状态 `正常 → 逾期`，标签 `version 1→2`、更新状态 `synced → outdated`。
这正好把 v0.4 文档第 5.5 节标注的风险演出来了：**周期一改，昨天还正常的设备今天就逾期**，且屏幕上的倒计时立刻失效需要重新下发。

高湿联动：改驾驶室湿度 64%→88% → 环境告警 2→3、总告警 13→14 → 应用联动后滑油滤器与导航雷达周期 30→15 天。压载泵因周期已是 7 天（下限）被自动排除。

### 结构说明页改为自动生成

把文档解析抽到 `prototype/docparse.py`，`build.py`（原型）与新增的 `build_structure.py`（结构页）共用。结构页的**模块地图**与**模块功能清单**两段现在由 v0.4 文档生成，夹在 `<!--MAP-->` / `<!--SPECS-->` 标记之间；其余段落仍手工维护。

这样两个交付物永远同源同 id，不会再各自漂移。页面从 5 模块扩到 **9 个模块 / 114 条需求**，锚点跳转与展开联动实测正常。

### 验证

| 项 | 结果 |
|---|---|
| 17 个视图 | 全部渲染，无控制台错误 |
| 390px 移动端 | 两个 HTML 均无横向溢出 |
| 61 个需求演示入口 | 全部命中，业务数据未变 |
| 改周期 / 改实测值 / 高湿联动 / 告警推进 | 逐项实测，联动数字正确 |
| 结构页锚点与展开 | 模块卡链接可展开对应清单并定位 |

## ③ 文档切 v0.4 与需求映射重建（2026-09-15，已完成并验证）

### 顺手把索引脆弱性根治了

旧方案 `coverage` / `requirementDemos` 用**零起始的模块/条目索引**（`'1-3'`），文档表格增删一行就整体错位，而且是**静默错位**——构建照样成功，只是每条需求都指错功能。既然本来就要重建映射，就一次改成 **id 键**：

```
coverage.storage = ['space/四级目录管理','space/目录概览', ...]
requirementDemos['labels/批量导入'] = {route:'labels/data', open:'import', ...}
```

id = 模块 slug + 功能名。配套在 `build.py` 里加了**构建期校验**：`coverage` / `requirementDemos` / `sketchReqs` 引用的 id 必须都能在文档中找到，否则构建直接失败并列出失配的 id。

实测过两种改动：

| 改动 | 结果 |
|---|---|
| 把「四级目录管理」改名为「空间目录管理」 | 构建**失败**，明确报出 `space/四级目录管理`，退出码 1 |
| 在表格中间插入一行 | 构建**通过**，需求 114→115，其余映射不受影响 |

即：从此只有改功能名才会失配，而失配会在构建时炸出来，不会再静默产出错页面。

### 提取范围

`build.py` 从 v0.4 文档第 5 章（模块功能范围）与第 6 章（二期）提取，共 **114 条需求**，覆盖 13 个 slug。表格列数不一（2/3/4 列）已统一归一化：首列为标题，其余列拼成「列名：值」。

`markdown()` 补齐了 v0.4 用到的语法：`**加粗**`、`~~删除线~~`、有序列表、`---` 分隔线、`>` 引用、文档内链接降级为纯文本。完整文档弹窗实测渲染 13 个 H2、32 张表、6 段代码块，无残留 Markdown 符号。

### 需求面板

- 版本行改为「产品文档 v0.4 / 章节 · **本页覆盖 n/m**」。
- 每条需求带自己的章节号（如 `5.4.2 看板中心 · 业务看板 · 01`），混排多模块时不会张冠李戴。
- 三种状态：**可点击演示** / **查看简化演示** / **原型未实现**（灰标）。最后一种是新加的——v0.4 写入但原型还没做到的需求会明确标出来，不再假装覆盖。

各页覆盖率：看板中心 17/20、仓储管理 5/7、标签台账 3/5、模板设计 2/6、标签状态 8/8、字段与数据 5/5、更新任务 3/3、标签策略 3/3、系统管理各页 1/2~3/3。

演示入口从 36 个增加到 **51 个**，全部实测命中目标，业务数据未变。

全文档 114 条中原型引用 63 条，其余 51 条属点检引擎页、环境监测页、移动端与二期调试——都是步骤 ④⑤ 的范围。

### 一处仍不一致

`product-structure.html` 的模块结构仍是 v0.1 的五模块（仓储/标签/标签状态/全船看板/系统管理），标题也仍标 v0.1。它的目录层级已改四级，但整页按 v0.4 重排属步骤 ④。**没有假装对齐**——标题保留 v0.1 是诚实标注，不是遗漏。

## ①② 点检数据模型与三层角色看板（2026-09-15，已完成并验证）

**为什么先做数据模型**：v0.4 看板约八成指标是点检数据的视图，而原数据模型里点检字段一个都没有。先捏临时字段再做看板，等真做点检引擎时聚合逻辑、钻取链路、状态色阶都要推翻重来。

### 数据模型（数据结构版本 `2` → `3`）

| 新增 | 说明 |
|---|---|
| `db.demoNow` | **演示基准时间 `2026-09-15 10:00:00`**。点检状态全部相对它推算，原型在任何日期打开都呈现同一套状态，不会随真实时间漂移 |
| `assets.cycle` / `lastCheck` / `fault` | 保养周期（天）/ 上次点检时间 / 故障报修中 |
| `db.inspections` | 点检流水：执行人、时间、结果、备注、是否拍照 |
| `db.envPoints` | 舱室级温湿度采集点与阈值 |
| `users.view` | 看板视角（exec / supervisor / worker / admin），与 `role` 功能权限是**两个维度** |
| `settings.dueSoon` | 临期阈值，默认 24 小时 |

点检状态（正常 / 临期 / 逾期 / 未点检）**是派生值，不落库**——由 `cycle + lastCheck` 相对 `demoNow` 推算，避免状态字段与时间字段不一致。

示例数据分布：正常 4 / 临期 2 / 逾期 2 / 未点检 1，完成率 67%，完好率 89%，环境异常 2。分段与班组完成率都有梯度，便于演示排名和钻取。

**环境示例埋了一个点**：主机舱 38.2°C / 72%RH、双层底 85%RH，都已超出候选标签自身 0-40°C / 45-70%RH 的工作范围。主管视图的环境面板底部直接写明了这条——被监测环境超出监测终端工作范围，是 v0.4 文档第 10.2 节的选型待决项。

### 看板中心

`全船看板` → `看板中心`，顶部三档角色切换器（不是三个独立页面：一键切换才能演示"同一份数据、三个视角"）。

钻取链路实测可用：`中高层 → 点分段 → 主管视图（该分段，下属舱室完成率）→ 点设备 → 工人视图（聚焦该设备）`。主管视图点班组则筛选该班组全部设备。

**闭环**：工人视图「完成点检」→ 写点检流水 → 截止日按保养周期后移 → **关联标签 `version++` 标记为待更新** → 可在标签管理创建下发任务。这把 v0.1 就确立的"标签在线 ≠ 屏幕已更新"原则变成了可点击的演示，而不只是文档里的一句话。

### 改动文件

`prototype/app.js`（seed、点检/环境派生函数、`overview()` 重写为 `boardExec/boardSup/boardWorker`、`checkForm`、看板动作与表单处理、账号页看板视角列、基础设置临期阈值）、`prototype/styles.css`（角色切换器、钻取路径、五列指标、低完成率橙条）。顺带删掉了已无引用的 `open-folder` 分支。

### 验证（浏览器实跑）

| 项 | 结果 |
|---|---|
| 13 个视图 + 3 个看板角色 | 全部渲染，无控制台错误 |
| 390px 移动端 | 三个角色视图均无横向溢出 |
| 指标 | 完好率 89% / 完成率 67% / 逾期 3 / 在线率 78% / 环境异常 2，与数据一致 |
| 钻取 | 分段 E101 → 下属舱室（辅机舱 0% / 主机舱 75%）→ 主发电机执行视图，四级定位正确 |
| 完成点检闭环 | 逾期→正常、流水 +1、异常自动带拍照标记、标签 version 1→2 且状态转 `outdated`、中高层完成率 67%→78% 联动 |
| 报修开关 | 标记 / 解除均生效 |
| 36 个需求演示入口 | 全部命中（已为看板路由加了视图复位），业务数据未变 |

### 已知不一致（等步骤 ③ 解决）

右侧需求对照面板仍读 v0.1 文档，看板中心对应的仍是「4.4 全船看板」那 7 条，没有三层角色、点检引擎、环境监测的需求原文。已在面板版本行标注「v0.4 已重构为看板中心」。

## 目录四级改造（2026-09-15，已完成并验证）

模型：`root` 节点 = Hull No.（一级），其下目录 depth 1/2/3 = 分段 / 舱室 / 部位。`MAX_DIR_DEPTH=3`，设备资产不占层级，可挂舱室也可挂部位（跳级）。

改动文件：

| 文件 | 内容 |
|---|---|
| `prototype/app.js` | 新增 `LEVELS` / `MAX_DIR_DEPTH` / `levelName()` / `levelChip()`；示例数据重建为 4 分段 / 6 舱室 / 5 部位 + 9 资产（主配电柜、锚机演示跳级）；目录树带层级标签；目录表单按上级目录提示「新建为…」；新建/移动校验改四级；`path()` 根节点返回 Hull No.；列头「部位」→「所属目录」并补层级小字；CSV 表头「所属部位」→「所属目录」 |
| `prototype/styles.css` | 新增 `.tree-node .level` 层级标签样式 |
| `prototype/shell.html` | 侧栏船号改为 Hull No. H2601 |
| `产品需求大纲-v0.1.md` | 只改目录层级相关的**行内文字**，未增删表格行（否则需求索引错位）；抬头注明该条款已于 2026-09-15 修订 |
| `product-structure.html` | 层级示例树、示例说明、功能表首行、表下注、草案清单同步四级 |

**数据结构版本 `1` → `2`**（现已再升到 `3`，见上节），`localStorage` 键仍为 `ship-tag-prototype-v1`。旧数据会自动重新播种，用户此前的演示修改会被清空——这是四级目录必须的迁移，分享文件时说明一句。

验证（浏览器实跑，非推断）：

| 项 | 结果 |
|---|---|
| 13 个视图渲染 | 全部通过，390px 无横向溢出，控制台无错误 |
| 目录树层级 | Hull No. / 分段 / 舱室 / 部位 四层标签正确 |
| 跳级 | 主配电柜 @ 配电间、锚机 @ 锚链舱 直接挂舱室 |
| 在部位下新建 | 拦截，提示「部位」已是最末级目录 |
| 上级目录选项 | 正确排除部位层；显示「→ 新建为「舱室/部位」」 |
| 超限移动 | 分段 H201 → 主机舱 被拒，提示四级限制，数据未变 |
| 合法移动 | 压载系统部位 → 驾驶室 成功，层级仍为部位 |
| CSV 导入 | 新表头通过，可导入到舱室层（跳级）；旧表头正确报错 |
| 36 个需求演示入口 | 全部命中目标，业务数据未变 |
| 需求对照面板 | 首条已显示「四级目录管理」 |

注：需求演示入口实测为 36 个（`requirementDemos` 条目数）；此前记录的「39」是产品文档的功能条目数，两者口径不同。

已决策（2026-09-15，用户确认）：

- **目录改为四级**：Hull No. → 分段 Block → 舱室 Compartment → 部位 Position。部位层允许跳级（设备可直接挂三级舱室），设备资产与标签不占层级。v0.1 的三级是用户自拟草稿，已作废。
- 已同步到 v0.4 文档 4.1 节与 `PROJECT_MEMORY.md`。**原型与结构说明页尚未同步**，`prototype/app.js` 的目录模型、示例数据、层级校验与移动/删除保护逻辑都要按四级重做。

剩余阻塞项（v0.4 文档第 11 章）：产品是否改名、执行层视图与移动端的关系、标签状态明细页归属、告警中心范围、标签 NFC 是否落实、二期电流/PLC 采集方案是否立项。

---

## 在线发布（2026-09-15 更新）

- 仓库：https://github.com/d-dai1984/ship-tag-system ，**公开仓库**。
- 入口页：https://d-dai1984.github.io/ship-tag-system/
- 管理后台原型：https://d-dai1984.github.io/ship-tag-system/prototype.html
- 点检员移动端：https://d-dai1984.github.io/ship-tag-system/mobile.html
- 产品结构说明：https://d-dai1984.github.io/ship-tag-system/product-structure.html
- 发布源：`main` 分支根目录；`.nojekyll` 保持静态文件直出。`index.html` 已改为三个入口的导航页，不再自动跳转。
- 两个旧中文文件为兼容跳转页，请勿继续编辑为正式页面。

**本地工作目录不是 git 仓库**。提交方式：把仓库 clone 到临时目录，复制文件进去再提交推送。git 身份用全局配置的 `d-dai1984 / youthadster@gmail.com`，凭证在 macOS 钥匙串。

**不上传到公开仓库的内容**（见 `.gitignore`）：

- `docs/*.pdf` —— 三份厂商与技术参考原始资料。
- `docs/参考-*.md` —— 含外部 AI 对话原文的参考存档。仓库是公开的，这类原文默认不发布；需要随仓库分发时删掉该行即可。

最近一次提交：`0f7314f` Migrate prototypes to v0.4（21 个文件，+3187 / -327）。

## 1. 接手先看这里

用户先要求编写功能需求和产品结构大纲，随后要求转换为便于沟通的 HTML，再制作可对照产品文档浏览的管理系统原型。本轮最后两项功能修改已完成：

1. **结构说明页与原型顶部互链**，可直接切换。
2. **原型右侧“可点击演示／查看简化演示”已可点击**，会跳到对应页面并高亮具体控件，或直接打开演示表单；移动端自动收起右栏。

用户最新请求是整理项目记忆和 hand-off，本文和 `PROJECT_MEMORY.md` 已按当前阶段整理，`README.md` 同步更新。不要把早期的采购澄清任务误当作用户本轮要求继续执行的工作。

## 2. 最新交付物

| 文件 | 用途与现状 |
|---|---|
| [产品需求大纲 v0.1](船舶资产电子标签管理系统-产品需求大纲-v0.1.md) | 功能范围、产品结构、核心对象、流程和边界；共 39 项功能需求，仍是讨论稿。 |
| [产品结构说明页](product-structure.html) | 系统结构图、三级目录示例、业务流程、可展开功能清单；支持打印／存为 PDF，与原型互链。 |
| [管理系统交互原型](prototype.html) | 单文件 HTML，13 个主要页面视图、示例交互、右侧需求对照及完整产品文档。 |
| [原型使用与源码说明](prototype/README.md) | 本地数据、原型范围、源文件与重建方式。 |
| [.impeccable.md](.impeccable.md) | 用户已确认的浅色简洁方向及沟通页设计背景。 |
| [PROJECT_MEMORY.md](PROJECT_MEMORY.md) | 当前范围、用户确认事项、草案假设，以及保留的硬件与早期技术背景。 |

两个 HTML 必须放在同一目录并保留当前文件名，顶部的相对链接才能正常工作。各自均不依赖外部资源；可直接通过浏览器打开，无需部署服务器。

## 3. 当前产品结构

| 一级模块 | 页面视图／子功能 |
|---|---|
| 全船看板 | 全船资产、标签、目录分布、异常待办及设备查询 |
| 仓储管理 | 舱室／部位目录与下属资产、标签 |
| 标签管理 | 标签台账、标签分组、字段与数据、模板设计、更新任务、标签策略 |
| 标签状态 | 在线、低电量、弱信号、内容更新情况与标签详情 |
| 系统管理 | 网关管理、账号权限、基础设置、操作日志 |

合计 13 个主要页面视图。“系统管理”是支撑功能，用户明确要求的核心范围仍为前四个模块。

## 4. 用户确认与草案假设

### 已有明确依据

- 目录最多三级，按船内舱室或功能部位分类管理。
- 首版产品文档先写功能需求与产品结构，细节后续展开。
- 展示风格选择“浅色简洁，适合开会讲解和打印”。
- 用可点击原型对照产品文档沟通，比纯结构图更直观。
- 两个 HTML 要互链；需求演示入口要跳到对应功能。

### 仍需确认

- 船舶不占三级目录层级；资产和标签不占目录层级。
- 一台资产绑定一枚在用标签，分组允许跨目录及多重归属。
- 字段、模板、角色、指标、策略冲突和下发确认的正式规则。
- 具体页面交互是否成为正式产品方案。用户要求制作并迭代原型，不代表全部草案已经获得业务批准。

本期“仓储管理”是位置与资产分类，不是采购、出入库、库存核算。点检、NFC、移动端、工单、多船及 MES/EAM 暂列扩展；不是已取消，也不是已授权立即实现。

## 5. 原型的可操作范围

目前可体验：

- 三级目录新增、改名、移动和非空删除保护；按目录查看资产。
- 资产新增、编辑、归属迁移；标签登记、绑定、更换和解绑。
- 标签搜索、状态筛选、批量选择、跨目录分组和 CSV 导出。
- 自定义字段、CSV 导入校验与新增／更新资产。
- 简化文本模板编辑、实时预览、标题颜色与字段显隐、复制和应用。
- 策略条件、优先级、模板与刷新计划配置；手动模拟规则匹配。
- 下发任务、模拟回执、离线待确认、失败重试与内容版本变化。
- 网关信息、示例账号权限、基础参数和操作日志。

关键演示链路：

`仓储管理 → 选择部位 → 编辑资产 → 标签标记待更新 → 创建下发任务 → 模拟回执 → 标签状态／全船看板`

在线、使用状态和内容更新状态分别显示。标签在线不表示屏幕已更新；离线项保留待确认，恢复在线后可再次模拟回执。

### 明确未实现

无后端、真实认证授权、网关连接或现场标签控制。Excel、完整拖拽模板编辑器、图片／二维码／条形码素材编辑、真实定时执行和完整异常处理尚未实现。原型中的示例数值、阈值和任务状态不是性能指标或硬件验证结果。

## 6. 最新需求跳转实现

右侧展示当前页面关联的产品需求原文。按钮功能为导航与展示：

- 批量导入 → 打开 CSV 导入窗口。
- 模板设计 → 打开模板编辑与实时预览。
- 刷新计划 → 打开策略表单并定位计划字段。
- 更新异常 → 打开含失败项的任务详情，定位重试入口。
- 目录、列表、搜索、统计等 → 跳转并短暂高亮相应区域。

这些跳转不会自动保存、导出、下发或改变业务数据。需要执行动作时，用户继续点击相应功能控件。

实现位于 `prototype/app.js`：

- `coverage`：每个页面展示哪些需求条目。
- `requirementDemos`：需求与页面、控件、演示窗口的映射。
- `openRequirementDemo`／`revealRequirement`：导航后打开或定位目标。
- `pendingRequirement`：等待路由完成后再执行定位，避免跳到旧页面。

## 7. 修改与重新生成

### 交互原型

修改 `prototype/` 中的源文件，随后在项目根目录运行：

```bash
python3 prototype/build.py
```

构建仅使用 Python 标准库，自动将样式、脚本、产品需求原文和完整产品文档嵌入根目录的原型 HTML。

| 源文件 | 修改内容 |
|---|---|
| `prototype/app.js` | 示例数据、页面、交互、需求跳转映射 |
| `prototype/styles.css` | 样式、响应式、跨页链接和目标高亮 |
| `prototype/shell.html` | 应用外壳与顶部结构说明页链接 |
| `prototype/docparse.py` | v0.4 文档解析：需求提取与 Markdown 渲染，被下面两个构建脚本共用 |
| `prototype/build.py` | 交互原型打包 + 构建期 id 校验 |
| `prototype/build_structure.py` | 重建结构说明页的模块地图与功能清单 |
| `prototype/build_mobile.py` | 打包移动端原型 + 移动端需求实现状态校验 |
| `mobile/shell.html` `styles.css` `app.js` | 移动端源码 |

**不要只编辑生成后的原型 HTML**，否则下一次构建会覆盖修改。

构建器从 **v0.4 文档第 5、6 章**提取需求，映射采用 **id 键**（模块 slug / 功能名），不再依赖表格顺序。插入或删除表格行是安全的；**改功能名会让构建直接失败**并列出失配 id，按提示同步 `coverage` / `requirementDemos` / `sketchReqs` 即可。

### 产品结构说明页

模块地图与功能清单两段由脚本生成，改完文档后运行：

```bash
python3 prototype/build_structure.py
```

生成内容夹在 `<!--MAP-->…<!--/MAP-->` 与 `<!--SPECS-->…<!--/SPECS-->` 之间，**不要手工编辑这两段**。其余段落（架构说明、目录示例、业务流程、边界、样式）仍为手工维护，脚本不会动。

文档内容变化时，跑一次 `build.py` 和一次 `build_structure.py` 即可，两者共用 `docparse.py`，不会再出现两边不一致。

### 本地数据与分享

- 使用 `localStorage`，键为 `ship-tag-prototype-v1`，数据结构版本当前为 `4`（`1`→`2` 四级目录，`2`→`3` 点检与环境数据模型，`3`→`4` 网关指标 / 系统健康度 / 告警处理状态；版本不符会自动重新播种，旧演示修改会被清空）。
- HTML 源码更新不会自动清除已有演示修改。需要恢复示例时，使用页面右上角“重置演示”；不要无故替用户清空。
- 分享 HTML 文件不会带上当前浏览器中的编辑结果；不同浏览器也不会同步。
- 在文件系统访问不同的电脑上，直接使用其本地文件路径；不要硬编码本机用户目录。

## 8. 已完成的验证

以下为前面实施回合的实际检查记录，本轮整理记忆未重复运行浏览器测试：

| 范围 | 验证结果 |
|---|---|
| 结构说明页 | 模块跳转、展开／收起、39 项功能保留、390px 手机无横向溢出、打印展开与分页检查通过。 |
| 原型页面 | 13 个主要视图正常渲染，受检桌面／手机页面无页面级横向溢出；浏览器未记录脚本错误。 |
| 主要数据链路 | 资产编辑与版本变化、一对一更换绑定、三级目录保护、批量下发、离线保留、恢复在线后回执、模板预览与保存通过。 |
| 数据管理与导航 | CSV 校验／导入、重复编码拦截、重载保持数据、分组维护、策略运行、取消不保存、完整文档、分组跳转及同页演示路线通过。 |
| 两页互链 | 已检查两个相对链接目标文件均存在。 |
| 最新需求演示按钮 | 逐个点击当前页面中的 39 个入口，目标路由、控件与高亮均正确；全过程业务数据未变，手机端需求栏自动收起。 |

“39 个入口通过”是导航验证，不代表 39 项需求已全部生产实现；不同页面可能引用相同需求。

临时验证脚本位于 `/tmp/ship-tag-prototype-check.cjs`、`/tmp/ship-tag-requirements-check.cjs`，截图位于 `/tmp/ship-tag-html/`。它们不保证在新电脑或清理临时文件后存在。使用的是本机 Chrome 与 Playwright；如需重跑，要先检查运行时可用性。

## 9. 下一步怎么接

**当前请求已完成，没有必须继续执行的未完子任务。** 收到新反馈后，先打开现有原型和结构说明页，在当前成果上增量修改。

下一轮可细化的产品问题：

1. 对齐目录层级与资产绑定关系。
2. 明确字段、导入规则、模板类型与样例数据。
3. 细化策略组合、优先级、刷新时机及失败处理。
4. 确认角色权限、首期看板指标和页面交互。
5. 需要正式开发时，再补接口、数据模型和验收口径。

这些是后续候选工作，不是用户已要求本轮执行的任务。当前没有阻止继续做产品文档或原型迭代的外部条件。

## 10. 保留的硬件与技术交接

此前已经阅读三份 PDF，型号参数、原始文件指纹和适配差距完整保存在 `PROJECT_MEMORY.md`。STag29AQ 仍是 PoC 候选，未完成采购选型。

真实接入／采购前仍缺少：现场环境与危险分区、标签量和并发目标、厂商 API／回执文档、私有部署方案、网关离线机制、样机验证及 MES/EAM 接口边界。

主要已知限制：

- 规格未给出 IP 等级，工作温湿度范围窄，电子纸易碎，缺少船厂环境适应性证据。
- NFC 为可选项，防克隆、近场防假检及跨平台离线链路尚未证明。
- 5 年续航是特定温度和刷新频率下的理论值；网关覆盖须在金属舱室实测。
- “私有化、API/SDK、断网存储”等宣传不能视为 Docker、REST/MQTT、30 天缓存或可靠刷新回执的承诺。

若后续用户进入真实接入或采购阶段，可继续准备厂商澄清表、正式 SRS、PoC 计划、系统上下文与离线状态机。未有新证据前，不升级现有硬件结论。

## 11. 下一位接手者的最短阅读路径

`PROJECT_MEMORY.md → HANDOFF.md → 产品需求大纲 v0.1 → 两个 HTML → prototype/README.md`

涉及代码时再读 `prototype/` 源文件；涉及硬件适配时再读三份 PDF。用户下一条反馈优先于旧的任务建议；文档、原型假设与硬件承诺必须分别看待。
