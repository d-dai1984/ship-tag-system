# 船舶资产电子标签管理系统

产品需求大纲 · v0.1 · 2026-09-14 · 讨论稿

## 1. 产品定位与本版范围

面向船舶资产管理员、舱室及设备负责人、标签运维人员和管理者，提供统一的电子标签管理后台，实现船内资产按位置归类、标签内容统一维护、远程批量更新、运行状态监控和全船信息汇总。

本版围绕仓储管理、标签管理、标签状态、全船看板四个核心模块定义功能范围和产品结构，补充支撑运行所需的网关、账号权限和操作记录。暂不展开页面布局、字段字典、交互流程、接口和验收指标。

本文中的“仓储管理”指船内舱室、功能部位及其下属资产和标签的分类管理。本期不包含采购、出入库、库存核算等传统仓储业务。

本轮用户需求作为本版范围依据；参考截图用于借鉴管理方式，原有技术要求用于保留业务背景，厂商资料用于识别硬件能力和约束。参考文件中的要求不自动作为本期必须交付的功能。

## 2. 核心管理对象

| 对象 | 定义与关系 |
|---|---|
| 船舶 | 当前管理范围；初版围绕单船展开，后续可扩展多船管理。 |
| 舱室／部位目录 | 船内资产及标签的主归属，最多三级；船舶作为根对象，不占目录层级。 |
| 设备资产 | 业务信息载体，保存设备身份、责任信息及其他自定义字段，归属一个主目录。 |
| 电子标签 | 安装在现场的显示硬件，具有独立标识、型号和运行状态，通过绑定关系展示资产信息。 |
| 标签分组 | 面向批量操作的集合，可跨目录按设备类型、责任班组、维护用途等组织。 |
| 字段与模板 | 字段定义展示的数据，模板定义这些数据在电子标签上的呈现方式。 |
| 标签策略与下发任务 | 策略决定何时、对哪些标签应用什么内容；任务记录实际执行进度和结果。 |

建议初版按“一台资产绑定一个在用标签，一个在用标签对应一台资产”设计，待业务确认；预留未绑定标签的管理能力。资产和标签不计入目录层级。

## 3. 产品结构大纲

```text
船舶资产电子标签管理系统
├── 全船看板
│   ├── 资产与标签总览
│   ├── 舱室／部位分布
│   ├── 标签运行与内容更新概况
│   └── 异常与待处理事项
├── 仓储管理（舱室／部位）
│   ├── 三级目录管理
│   ├── 目录概览
│   ├── 下属资产与标签数据
│   └── 归属调整
├── 标签管理
│   ├── 标签台账与资产绑定
│   ├── 标签分组
│   ├── 字段配置与信息录入
│   ├── 批量导入／导出
│   ├── 模板设计与素材管理
│   ├── 信息下发与更新任务
│   └── 标签策略与刷新计划
├── 标签状态
│   ├── 状态总览与标签列表
│   ├── 单标签状态详情
│   ├── 异常记录与处理
│   └── 状态查询／导出
└── 系统管理（支撑功能）
    ├── 船舶基础信息
    ├── 网关管理
    ├── 账号、角色与数据权限
    ├── 基础参数
    └── 操作日志
```

仓储管理与标签管理中的列表使用同一套资产、标签及绑定数据：前者以目录为入口，后者支持全船查询与批量操作。

## 4. 功能需求大纲

### 4.1 仓储管理：按舱室和功能部位组织资产

参考截图 1 的管理入口及截图 5 的下属数据列表。

| 功能 | 初版需求 |
|---|---|
| 三级目录管理 | 支持目录新增、编辑、排序、移动与删除；最多三级，目录可按舱室或功能部位命名。 |
| 目录基础信息 | 维护目录名称、编号、类型、说明及责任信息。 |
| 目录概览 | 查看当前目录及下级目录的资产数量、标签数量、绑定情况和异常概况。 |
| 下属数据管理 | 查看和维护当前目录下的资产信息、已绑定标签及关联模板，支持搜索、筛选与自定义显示列。 |
| 归属调整 | 支持资产及其关联标签的单个、批量迁移；调整归属时保持资产与标签的关联。 |
| 结构维护保护 | 调整或删除目录时处理其下属目录与数据，避免资产、标签失去管理归属。 |

层级示例：`某船舶 → 动力区域（一级）→ 主机舱（二级）→ 燃油供给部位（三级）→ 设备资产及标签`。示例不要求所有目录都建满三级，也不固定各级名称。

### 4.2 标签管理：管理标签、数据、模板和更新

参考截图 2 的标签分组与设置入口、截图 3 的条件策略，以及截图 5 的数据维护功能。

| 功能 | 初版需求 |
|---|---|
| 标签台账 | 管理标签唯一标识／MAC、型号、屏幕尺寸、支持颜色、所属目录及使用状态；支持单个、批量登记与查询。 |
| 资产绑定 | 支持绑定、解绑和更换标签，查看关联资产及绑定记录；支持待绑定、在用、停用等管理状态。 |
| 标签分组 | 新增、编辑、删除分组，维护成员，查看数量；按分组执行模板应用、信息更新等批量操作。 |
| 字段配置 | 配置资产及标签显示所需字段，包括字段名称、类型、必填与否等基础属性。 |
| 信息录入 | 支持逐条录入、编辑及批量修改；可维护设备编码、名称、型号、位置、责任班组、责任人等信息。具体字段后续细化。 |
| 批量导入 | 提供导入模板，支持 Excel／CSV 数据导入、字段匹配、数据校验和结果反馈；支持新增与更新既有记录。 |
| 数据导出 | 按目录、分组或筛选结果导出资产和标签业务数据。 |
| 模板设计 | 按标签尺寸、分辨率和颜色设计版式；支持文本、图片、二维码、条形码和图形元素，并与业务字段关联。 |
| 模板管理 | 支持模板新增、编辑、复制、预览、版本管理及应用范围查看，使用实际数据预览显示效果。 |
| 素材管理 | 管理模板使用的图片、图标等公共素材。 |
| 信息下发 | 支持单个、批量、按目录或分组下发；支持首次发布及数据、模板变更后的更新。 |
| 更新任务 | 查看任务进度、标签执行结果和失败原因，支持失败重试与历史记录查询。 |
| 标签策略 | 根据业务字段条件及标签尺寸、颜色等适配条件选择模板；支持条件组合、优先级、默认模板、启停及应用范围。 |
| 刷新计划 | 支持即时、定时和周期刷新；策略匹配与刷新计划共同决定更新内容和执行时机。 |
| LED 控制 | 支持单个、批量点灯，用于现场寻标或提示；具体颜色、闪烁方式和策略联动能力以硬件接口为准。 |

策略示例：某类设备匹配专用模板，不同屏幕尺寸匹配各自版式；若后续提供有效的业务状态数据，可按该状态切换提醒模板。本期不因此自动包含点检规则引擎。

### 4.3 标签状态：掌握硬件运行与更新结果

参考截图 4 的 MAC、尺寸、RSSI、电量、在线状态及最后广播时间。

| 功能 | 初版需求 |
|---|---|
| 状态列表 | 汇总展示标签标识、关联资产、所属目录、尺寸、RSSI、电量、在线状态、最后广播／心跳时间及内容更新情况。 |
| 状态筛选 | 按目录、分组、型号、绑定情况、在线状态、电量及更新结果筛选。 |
| 单标签详情 | 查看基础信息、最近上报数据、关联资产与网关、当前模板及下发历史。 |
| 运行异常 | 识别并汇总离线、低电量、弱信号等异常，支持查看、处理标记及恢复记录。 |
| 更新异常 | 查看下发失败、超时、尚未同步等情况，关联原任务并支持重新下发。 |
| 查询与导出 | 支持刷新后台状态数据及导出筛选结果，明确最近数据更新时间。 |

状态分为三类，分别展示：

- 使用状态：待绑定、在用、停用等。
- 通信与健康状态：在线／离线、电量、信号及数据新鲜度。
- 内容更新状态：待下发、执行中、已确认更新、失败或结果待确认等。

“标签在线”不能代表屏幕内容已经更新；仅有任务发送成功时，不能显示为屏幕更新成功。实际确认能力需根据厂商回执接口确定。

### 4.4 全船看板：汇总全船设备与标签信息

| 功能 | 初版需求 |
|---|---|
| 全船总览 | 展示资产总数、标签总数、已绑定与未绑定数量，以及已配置标签的资产占比。 |
| 运行概况 | 汇总在线、离线、低电量、弱信号标签数量及分布；各类异常可能重叠，分别统计。 |
| 更新概况 | 展示待更新、执行中、已确认更新、失败及结果待确认的任务或标签情况。 |
| 舱室／部位分布 | 按目录汇总资产、标签和异常，支持逐级展开，定位到具体部位及标签。 |
| 全船信息查询 | 按资产名称／编码、目录、分组及状态查询全船设备标签信息。 |
| 异常与待办 | 集中展示待绑定、离线、低电量、更新失败等需要处理的事项，并进入对应管理页面。 |
| 数据时效 | 展示统计范围及最近更新时间，支持手动或自动刷新。 |

本版使用统计卡片、图表和目录分布表达全船情况；船舶平面图、三维模型和实时定位作为可选扩展。设备完好率、点检完成率等指标，需要接入相应业务数据后再纳入。

### 4.5 系统管理：支撑功能

| 功能 | 初版需求 |
|---|---|
| 船舶信息 | 维护船名、编号等当前船舶的基础信息。 |
| 网关管理 | 维护网关身份、安装位置、连接状态及关联标签，辅助定位通信问题。 |
| 账号与权限 | 管理账号、角色、功能权限和目录数据范围，区分管理员、操作人员与只读查看者。 |
| 基础参数 | 配置状态判定、异常提醒和默认刷新等参数，具体阈值后续定义。 |
| 操作日志 | 记录目录调整、数据导入修改、绑定变更、模板策略修改及下发操作。 |

## 5. 核心业务链路

`建立舱室／部位目录 → 录入或导入资产信息 → 登记并绑定标签 → 配置分组与模板 → 手动或按策略下发 → 查看执行结果与标签状态 → 全船看板汇总`

资产信息或模板发生变化后，形成新的更新任务；执行失败或结果未确认时保留待处理事项，便于持续跟进。

## 6. 参考资料带来的设计约束

| 资料依据 | 对本版产品设计的影响 |
|---|---|
| STag29AQ 规格书 PDF 第 2–4 页 | 候选标签为 2.9 英寸、296 × 128 像素、黑白红黄四色，支持 BLE 通信、刷图与点灯；模板应按实际屏幕能力适配。 |
| STag29AQ 规格书 PDF 第 3 页 | NFC 为可选项，初版核心管理流程不依赖 NFC；按键事件的业务用途留待接口确认。 |
| 厂商介绍 PDF 第 46 页 | 自定义模板、二维码、条形码、图片及色块可作为模板功能参考；不同型号能力需分别适配。 |
| 厂商介绍 PDF 第 49、51 页 | 平台、API／SDK、网关和内网私有部署可作为接入方向，具体接口、回执、离线补发及部署条件尚需确认。 |
| 原有《技术要求》PDF 第 1–2 页 | 保留资产归属、标签绑定、状态监控和看板背景；点检、移动端、工单及 MES／EAM 对接列为后续扩展。 |

上述资料用于支撑需求设计，尚不代表相关能力已完成项目验证。涉及硬件反馈的功能应在接口确认后细化。

## 7. 后续细化事项

- 确认目录层级命名，以及船舶不占三级目录层级的口径。
- 确认一台资产与标签的绑定数量关系、跨目录分组方式。
- 确认首批标签型号、设备字段及需要的模板类型。
- 细化策略触发、优先级、默认模板与手动下发之间的关系。
- 明确下发回执、在线判定和异常处理口径。
- 确认内网部署条件、网关接入方式及首期管理规模。

扩展方向：多船管理、点检计划与记录、NFC／按键现场交互、移动端离线作业、报修工单、MES／EAM 对接及船舶图形化定位。以上暂不计入本版核心范围。
